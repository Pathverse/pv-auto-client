name: pv_auto_client
description: generate chopper client automatically from postman collection
version: 1.0.0

environment:
  sdk: ^3.10.1

executables:
  pv_auto_client:

# Add regular dependencies here.
dependencies:
  path: ^1.9.0           # Cross-platform file path manipulation
  http: ^1.2.0           # HTTP client for Postman API
  dcli: ^8.2.0           # CLI framework with command support
  yaml: ^3.1.2           # YAML parsing/generation for build.yaml

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6